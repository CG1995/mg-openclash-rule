# clash-rule

本仓库现在只维护 `Clash-MG.ini`。

本地依赖只有这 4 个规则文件：

- `AI.list`
- `Apple.list`
- `Proxy.list`
- `Adobe.list`

其他分类规则均来自 `blackmatrix7/ios_rule_script`，直接保持外链引用即可。

清理掉旧配置和历史遗留文件，是为了减少维护成本并避免仓库内容混乱。
